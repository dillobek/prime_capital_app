# Flutter/Dart/Pub
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.iml
*.ipr
*.iws

# IDE
.idea/
.vscode/

# Platform build outputs (the android/ and ios/ project folders themselves
# are generated once by `flutter create .` — see README.md — and should be
# committed like any normal Flutter app; only their own build artifacts are
# ignored here).
android/.gradle/
android/app/build/
android/local.properties
ios/Pods/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec
ios/.symlinks/
**/DerivedData/
